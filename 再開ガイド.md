# 日本語学習アプリ ／ 作業の再開ガイド

*最終更新: 2026-08-30*

---

## いまここ（現在地）

| 項目 | 状態 |
|---|---|
| 開発環境（Flutter / Android SDK） | ✅ 完了 |
| 実機（Pixel 8a）での起動 | ✅ 完了 |
| P0検証ツール（音声認識テスト画面） | ✅ 動作中 |
| 日本語フォント同梱 | ✅ 完了（中国語字形の問題を解消） |
| **P0検証（ネイティブ）** | ✅ 認識率ほぼ100% ＝ アプリ側は正常 |
| **P0検証（学習者）** | ⬜ 次にやること。生徒さん3〜5人 |

プロジェクトの場所： `C:\src\nihongo_app`

### 設定で踏んだ地雷（再発したら思い出す用）

| 症状 | 原因と対処 |
|---|---|
| `flutter` が認識されない | 解凍でフォルダが二重に。正しくは `C:\src\flutter\flutter\bin` |
| `アプリケーション制御ポリシー〜` の無限ループ | Windows 11 スマートアプリコントロール。**一度オフにすると戻せない**ので慎重に |
| `Android license status unknown` | Command-line Tools 23.0 の不具合 |
| `sdkmanager did not install NDK` | 同上。**Command-line Tools を 22.0 に下げて解決済み** |
| 漢字が中国・台湾の字形になる | フォント未同梱。`assets/fonts/NotoSansJP-*.otf` で解決済み |
| `No MaterialLocalizations found` | `locale: ja` を指定したのに翻訳データが無かった。指定を削除して解決 |

---

## 次回の始め方（これだけ）

1. **スマホをUSBでPCに繋ぐ**（USBデバッグの設定は済んでいるので、挿すだけ）
2. **PowerShell を開く**
3. 次の3行を打つ

```powershell
cd C:\src\nihongo_app
flutter devices
flutter run
```

`flutter devices` に **Pixel 8a** が出ていることを確認してから `flutter run` してください。
2回目以降の起動は1〜2分で終わります。

### スマホが出てこないとき

- ケーブルを挿し直す
- 通知パネルを下ろして USB接続を「**ファイル転送**」に切り替える
- スマホに「USBデバッグを許可しますか？」が出たら **許可**

---

## 実行中に使うキー

`flutter run` している PowerShell で押します。

| キー | 動作 | いつ使うか |
|---|---|---|
| `r` | ホットリロード（1秒） | **コードを変えたとき（ほぼこれ）** |
| `R` | ホットリスタート（数秒） | 画面が変にならないとき、状態をリセットしたいとき |
| `q` | 終了 | 作業を終えるとき |

> **`pubspec.yaml` を変えたときだけは `q` → `flutter run` で入れ直し**が必要です。
> フォントやパッケージを足したときが該当します。

---

## Git（バックアップ）の使い方

### 最初の1回だけ

**① 名前とメールを登録**（未設定だと commit が失敗します）

```powershell
git config --global user.name "ayu"
git config --global user.email "hifive3321@gmail.com"
```

**② リポジトリを作って、いまの状態を保存**

```powershell
cd C:\src\nihongo_app
git init
git add .
git commit -m "P0検証ツールが動くところまで"
```

> `.gitignore` は `flutter create` が用意済みです。`build` フォルダなど巨大で不要なものは
> 自動で除外されるので、気にしなくて大丈夫です。

### これから毎回（動いたと思ったら、その都度）

```powershell
git add .
git commit -m "何をしたか日本語で書く"
```

**「動いた」と思った瞬間に毎回やるのがコツ**です。1日1回ではなく、
「マイクが動いた」「判定が直った」など、小さな成功ごとに保存します。

### 保存した履歴を見る

```powershell
git log --oneline
```

### 壊れたので戻したい

```powershell
git log --oneline                  # 一覧から戻りたい地点の英数字をメモ
git checkout <その英数字> -- .      # ファイルをその時点に戻す
```

全部やり直したい（未保存の変更を全部捨てる）ときは：

```powershell
git restore .
```

### PCが壊れても消えないようにする（GitHub）

1. https://github.com でアカウントを作る
2. 右上「＋」→ New repository → 名前 `nihongo_app` → **Private を選ぶ** → Create
3. 画面に出るコマンドをコピーして PowerShell に貼る

以降は保存のたびに次を追加するだけです。

```powershell
git push
```

---

## 次にやること

### 1. 学習者でのP0検証（最優先）

スクールの生徒さん3〜5人に、このアプリで20フレーズ話してもらいます。
**画面下の Recognition rate をメモするだけ**でOKです。

| 認識率 | 判断 |
|---|---|
| 80%以上 | 音声中心の設計でよい。予定通り進める |
| 60〜80% | 判定を甘めに調整＋入力方式の切替を目立たせる |
| 60%未満 | 設計変更。選択式やローマ字入力を主にする |

**この数字が出るまで、本格的な開発には進まないでください。**
ここがアプリの成否を決めます。

### 2. その後の予定

- 出題→音声入力→判定→結果 の1レッスン分を作る
- カレンダー・統計・復習の画面
- N3〜N1の単語リスト作成（N5・N4は完成済み）
- 第4週以降の例文（第1〜3週 399問は完成済み）

---

## 手元にある資産

| ファイル | 中身 |
|---|---|
| 日本語学習アプリ_仕様書.md | 全体設計。画面・データ構造・課金・判定ロジック |
| 開発ガイド_Windows初心者向け.md | 環境構築とトラブル対処 |
| 第1〜3週例文集（Day001-021） | 399例文。既習事項チェック済み |
| JLPT_N5_単語リスト | 803語 |
| JLPT_N4_単語リスト | 1,644語 |
| 既習事項チェッカー.py | 例文の文法順序を自動検査 |
| 照合ロジック_プロトタイプ.py | 判定ロジックの検証用 |

すべて claude.ai のプロジェクト「スマホ用アプリ開発」に保存済みです。
